<h1>New page</h1>

<% form_for(@page) do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.text_field :header %>
  </p>
  
  <p>
    <%= f.text_area :pagetext %>
  </p>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', pages_path %>