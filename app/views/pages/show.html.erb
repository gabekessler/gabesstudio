<% title "#{@page.header}" %>

<h1><%= yield(:title) %></h1>

<% if @page.image? %>
  <%= image_tag @page.image.url(:long), :class => "long_image" %>
<% end -%>


<p><%= @page.pagetext  %></p>
<%= link_to 'Edit', edit_page_path(@page) %> :
<%= link_to 'Back', pages_path %>