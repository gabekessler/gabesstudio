 <div class="toolbar"> 
       <h1 id="pageTitle"></h1> 
       <a id="backButton" class="button" href="#"></a> 
   </div>
  <ul id="<%= clean(@project.title) %>" title="<%= @project.title %>">
    <li><%= @project.text %></li>
    <li><%= @project.link %></li>
    <li><h2>Gallery Images</h2>
        <% for gallery_image in @gallery_images %>
            <%= gallery_image.image.url(:small) %>
        <% end %></li>
  </ul>