<h1>Listing projects</h1>

<table>

<% @projects.each do |projects| %>
  <tr>
    <td><%= link_to projects.title, project_path(:project) %></td>
    <td><%=h projects.project_type %></td>
    <td><%= link_to 'Edit', edit_project_path(projects) %></td>
    <td><%= link_to 'Destroy', project, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New projects', new_project_path %>